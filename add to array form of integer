class Solution:
    def addToArrayForm(self, num: List[int], k: int) -> List[int]:
        a=0
        ans=[]
        for i in num:
            a=a*10+i
        a=a+k
        while a>0:
            t=a%10
            ans.append(t)
            a//=10
        ans.reverse()
        return ans
