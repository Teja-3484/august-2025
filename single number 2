class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        d={}
        c=0
        for i in nums:
            if i in d:
                d[i]+=1
            else:
                d[i]=1
        for key,val in d.items():
            if val==1:
                return key
