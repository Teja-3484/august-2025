class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        a = []
        m = prices[0]
        for i in range(len(prices)):
            profit = prices[i] - m
            a.append(profit)
            if prices[i] < m:
                m = prices[i]
        return max(a)
