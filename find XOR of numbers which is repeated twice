class Solution:
    def duplicateNumbersXOR(self, nums: List[int]) -> int:
        d={}
        a=[]
        res=0
        for i in nums:
            if i in d:
                d[i]+=1
            else:
                d[i]=1
        for key,val in d.items():
            if val==2:
                a.append(key)
        for i in range(len(a)):
            res^=a[i]
        return res
