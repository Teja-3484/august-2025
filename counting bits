class Solution:
    def countBits(self, n: int) -> List[int]:
        a=[]
        for i in range(0,n+1):
            c=0
            i=bin(i)[2:]
            for j in i:
                if j=='1':
                    c+=1
            a.append(c)
        return a
